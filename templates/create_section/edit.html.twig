{% extends 'base.html.twig' %}

{% block title %}Modifier Section{% endblock %}

{% block body %}
    <h1>Modifier Section</h1>

    {{ include('create_section/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="connexion">
        <button class="connectbtn">
        <a href="{{ path('app_create_section_index') }}">Revenir à la liste</a>
        </button>

        {{ include('create_section/_delete_form.html.twig') }}
    </div>
{% endblock %}
