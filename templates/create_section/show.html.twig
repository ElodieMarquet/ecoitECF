{% extends 'base.html.twig' %}

{% block title %}Section{% endblock %}

{% block body %}
    <h1>Section</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>            
            <tr>
                <th>Name</th>
                <td>{{ section.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_create_section_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_create_section_edit', {'id': section.id}) }}">Modifier</a>

    {{ include('create_section/_delete_form.html.twig') }}
{% endblock %}
